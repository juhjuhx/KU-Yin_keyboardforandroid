# C4-D2.5-RC Handoff (standalone; zero chat history assumed)

1. Repo: `juhjuhx/KU-Yin_keyboardforandroid`. 2. Branch: `c4-compose-libchewing-switch`
   (NEVER merge main@73e810f into it; NEVER rebase onto it).
3. Baseline: `e72acb3` (C4-D2: session wiring, Zhuyin wired). 4. Final: `34a758b`
   (e3b3097 + review-fix commit). 5. Chain: e72acb3 → e3b3097 (D2.5 ownership,
   13 files +541/-45) → 34a758b (review fixes, 4 files +37/-10).
6. Production arch: Service → session.dispatch → ZhuyinDictionarySession(engine)
   → Compose UI applies results; Engine owns mode/shift/policy only.
7. Non-production: ChewingEngineSession + AndroidChewingEngine + JNI (test/harness
   only until D3); old View shell = PR #4 donor. 8. Active decoder:
   ZhuyinDictionary. 9. D3 decoder: ChewingEngineSession + AndroidChewingEngine.
10. Owners: mode/shift/policy = KuYinEngine (+service preferred); composition,
    candidates, paging, selection, space/enter/backspace/punct/reset = session.
11. EditorPolicy: from() collapses inputType/imeOptions → sensitive/learn/
    candidates/composition/forceAscii/action; EffectiveModePolicy: forced →
    ENGLISH else preferred (preferred never overwritten by forced inputs).
12. DispatchResult(commitText, consumed, additionalCommits=[]): commitText =
    primary (composition flush), additionalCommits = ordered follow-ups
    (punctuation); UI applies commitText then additionalCommits in order;
    consumed=false means "service/UI must do default" (editor action, delete, noop).
13. Commands: TapZhuyinKey, SelectCandidate(index, stale→null/false),
    Backspace (empty→false→EditorDeletion), Space (empty→" "/active→decoder),
    Enter (active→commit once/empty→false), Punctuation(text) (flush→punct,
    empty→punct only), Complete (flush w/o editor action, for mode switch),
    PageForward/Backward, ToggleCandidatesExpanded, ClearComposing/Reset.
14. CASE-4: select → committedText≠"" possible + remaining preedit kept +
    next key continues, no reset (ChewingEngineCase4Test locks it).
15. CASE-6: (no CASE-6 definition exists in repo; treat as unknown — do not invent).
16. Device-only gates: real InputConnection effects, Unicode rendering, touch
    geometry, rotation/recreate, 200% zoom, dark theme, QR scan (donation card art).
17. Tests: `./gradlew testDebugUnitTest` (24 classes, 99 @Test: 61 pre-existing +
    D2.5 new 16 across Enter/Punct/ModeSwitch/EffectiveMode + RankingTest 10 etc.).
18. Counts: see 17 (JVM run pending on SDK box). 19. Build:
    `./gradlew assembleDebug`; APK → app/build/outputs/apk/debug/*.apk.
20. APK hash: NONE YET (no build here). 21. CI: build.yml runs 7 contracts +
    JVM + assembleDebug (blocking); runtime-smoke `continue-on-error: true`
    (experimental — never count as PASS).
22. Pins: prebuilt 3587ba33, libchewing a6a8fa4 (`scripts/bootstrap_native_deps.sh`);
    NDK 28.2.13676358, CMake 3.22.1, JDK 21, Gradle 9.3.1, AGP 9.1.1,
    Kotlin 2.2.10, compile/target 36, minSdk 24.
23. Security: zero permissions (no INTERNET), no HTTP libs, clipboard WRITE-only
    (feedback share, user gesture), logging = 3 native-error Log.e (no keystroke
    content), backup: allowBackup=false + noBackupFilesDir userdict.
24. Unresolved: JVM/APK/runtime evidence (no SDK here); switchMode unused param
    (kept for interface stability); dual learning (Zhuyin prefs + libchewing flag)
    pending B8 single-ranking decision; ROW_*/canonical dup pending M4.
25. D3 blockers: none from D2.5 code (gate = SDK GREEN + human authorization).
26. D3 first task: B1 native lifecycle owner (service constructs
    AndroidChewingEngine once, init DACHEN, close exactly once) + B2 fallback.
27. Forbidden: merge/rebase main, force-push, INTERNET/telemetry/Firebase,
    Rime/new framework, AAB/Play, PR #6, delete donor/fallback, package rename,
    Gboard asset copying, mega commits, D3 before D2.5 GREEN+authorization.
28. TAUI warning: main@73e810f adds taui-workspace/ (unrelated web prototype);
    recorded in docs/REPO-HYGIENE-2026-09-23.md; revert separately, never rewrite.
29. PR #4 donor role: legacy View IME shell kept as reference only, not product path.
30. PR #6 docs role: documentation-only PR, do not merge code through it.
