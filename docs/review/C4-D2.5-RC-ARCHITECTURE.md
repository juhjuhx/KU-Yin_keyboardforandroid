# C4-D2.5-RC Architecture inventory (from code @34a758b, NOT filenames)

Statuses: ACTIVE PRODUCTION / ACTIVE FALLBACK / TEST-ONLY / DONOR-LEGACY /
FUTURE C4 / DEAD.

```
Android editor
    │
InputConnection ── EditorPolicy.from() (ACTIVE PRODUCTION normalizer)
    │
KuYinInputMethodService (ACTIVE PRODUCTION)
    ├── userPreferredMode + EffectiveModePolicy (ACTIVE PRODUCTION, D2.5 new)
    ├── KuYinEngine (ACTIVE PRODUCTION: mode/shift/policy + fallback decode)
    ├── ComposeDecoderSession (ACTIVE PRODUCTION contract)
    │       ├── ZhuyinDictionarySession (ACTIVE PRODUCTION adapter)
    │       └── ChewingEngineSession (TEST-ONLY + FUTURE C4; fake-driven tests)
    ├── KuYinKeyboardUi (ACTIVE PRODUCTION; dispatches session, applies results)
    └── (NOT constructed in production) AndroidChewingEngine (TEST-ONLY + FUTURE C4)
            │  (only caller today: tests/harness; service wires Zhuyin session)
            JNI chewing_jni.cpp, 24 exports (ACTIVE BUILD, compiled into APK)
            │
        libchewing (pinned a6a8fa4; assets via bootstrap .deps, absent here)
```

Box notes: CompositionResetPolicy/EditorDeletion/KeyboardSettings = ACTIVE
PRODUCTION helpers. ImeSessionController + EditorPolicy live under
androidkeyboard/input (ACTIVE). SymbolPicker/KeyboardView = DONOR-LEGACY View
shell (PR #4, not product path). No DEAD production code found beyond already
removed items (metadata.json, UpstreamSyncManager, Firebase – see COMPOSE-MAINLINE).

## Source-of-truth audit (F)

| Truth | Source | Duplicates | Consistent | Risk |
|---|---|---|---|---|
| Dachen mapping+codes | `androidkeyboard/input/KeyboardLayout.kt` Dachen (+KeyMapping pinned to libchewing standard.rs) | `ime/engine/ZhuyinConstants.kt` ROW_* | NO – ROW_3/4 still old-wrong (ㄒ@g, missing ㄕㄖ) | Compose renders wrong keys; FIX = port local ROW fix (D3/M4 item, not this round) |
| Candidate/composition/mode state | `session.state` (D2.5) | engine fields (adapter-internal mirror, read only by Zhuyin session) | YES | None – UI/service grep-clean |
| Editor policy | `EditorPolicy.from()` + service `userPreferredMode` | none | YES | None |
| Learning | Zhuyin prefs AND libchewing auto-learn (dual) | – | NO (by design, pending) | B8 must pick one; documented, not fixed here |
| Symbols/emoji | hardcoded UI rows + EMOJI_LIST | quickPunct subset | Partial | M4 SymbolCatalog item |
| appId/service | `io.github.juhjuhx.kuyin` (build file) + manifest service (contract-pinned) | none found | YES | None |
| Native pin | bootstrap: prebuilt `3587ba33`, libchewing `a6a8fa4`; NOTICE consistent per COMPOSE-MAINLINE | none | YES (cited, not byte-reverified here) | None |
| Version | 0.2.0-alpha.1 / versionCode 20001 | release APK 278bcc2 = OLD (pre-C3.5) | Release mismatch recorded | Do not use release APK as branch truth |
| Toolchain | gradle 9.3.1, AGP 9.1.1, Kotlin 2.2.10, compile/target 36, minSdk 24, NDK 28.2.13676358, CMake 3.22.1, JDK 21, robolectric 4.16.1, JUnit4 | runtime-smoke uses NDK 27.3 (workflow file) | Minor mismatch (pre-existing) | Env-only; record, don't change here |
